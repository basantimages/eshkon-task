/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.ctfassets.net', 'upload.wikimedia.org', 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
